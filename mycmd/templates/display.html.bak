<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <title>Skymoons | SAplatform</title>
  <meta content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no" name="viewport">
  <link rel="stylesheet" href="/static/bootstrap/css/bootstrap.min.css">
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.4.0/css/font-awesome.min.css">
  <link rel="stylesheet" href="https://code.ionicframework.com/ionicons/2.0.1/css/ionicons.min.css">
  <link rel="stylesheet" href="/static/dist/css/AdminLTE.min.css">
  <link rel="stylesheet" href="/static/dist/css/skins/_all-skins.min.css">
  <link rel="stylesheet" href="/static/css/zTreeStyle/metro.css">
  <link href="/static/image/uploadify/uploadify.css" type="text/css" rel="stylesheet" />
  <script type="text/javascript" src="/static/plugins/jQuery/jQuery-2.1.4.min.js"></script>
  <script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/swfobject/2.2/swfobject.js"></script> 
  <script type="text/javascript" src="/static/image/uploadify/jquery.uploadify-3.1.min.js"></script>
  
  <script src="/static/js/jquery.ztree.all-3.5.min.js"></script>
</head>

<body class="hold-transition skin-blue sidebar-mini">
<div class="wrapper">

  <header class="main-header">
  
    <a href="/index/" class="logo">
      <span class="logo-mini"><b>A</b>LT</span>
      <span class="logo-lg"><b>Skymoons</b>SA</span>
    </a>

    <nav class="navbar navbar-static-top" role="navigation">
      <a href="#" class="sidebar-toggle" data-toggle="offcanvas" role="button">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </a>
    </nav> 
  </header>

  <aside class="main-sidebar">
    <section class="sidebar">
      <div class="user-panel">
        <div class="pull-left image">
          <img src="/static/dist/img/user2-160x160.jpg" class="img-thumbnail" alt="User Image">
        </div>
        <div class="pull-left info">
          <p>Skymoons</p>
          <a href="#"><i class="fa fa-circle text-success"></i> Online</a>
        </div>
      </div>
      <!-- search form -->
      <form action="#" method="get" class="sidebar-form">
        <div class="input-group">
          <input type="text" name="q" class="form-control" placeholder="Search...">
              <span class="input-group-btn">
                <button type="submit" name="search" id="search-btn" class="btn btn-flat"><i class="fa fa-search"></i>
                </button>
              </span>
        </div>
      </form>
      <!-- /.search form -->
      <!-- sidebar menu: : style can be found in sidebar.less -->
      <ul class="sidebar-menu">
        <li class="header">MAIN NAVIGATION</li>
        <li class="treeview">
          <a href="/index/">
            <i class="fa fa-dashboard"></i> <span>Dashboard</span> <i class="fa fa-angle-left pull-right"></i>
          </a>
        </li>
        <li>
          <a href="/general/">
            <i class="fa fa-th"></i> <span>Servers status</span>
            <small class="label pull-right bg-green">new</small>
          </a>
        </li>
        <li class="treeview">
          <a href="/dir/">
            <i class="fa fa-edit"></i> <span>Display files</span>
            <i class="fa fa-angle-left pull-right"></i>
          </a>
        </li>
      </ul>
    </section>
  </aside>

  <div class="content-wrapper">
    <section class="content">
      <div class="row">
        <!-- left column -->
        <div class="col-md-6">
          <!-- general form elements -->
          <div class="box box-primary">
            <div class="box-header with-border">
              <h3 class="box-title">更新文件</h3>
            </div>
            <!-- /.box-header -->
			            <form method="post" enctype="multipart/form-data">
<!--
                          <div class="form-group">
                                        <label for="id_upload_file">上传文件</label></br>
                                        {{ form.upload_file }}
                                        {{ field.errors }}
              </div>
-->
<!--下面的是前端测试通过的
              <h3>Uploadify组件上传方式</h3>
              <div class="demo-box">  
                  <input id="file_upload" type="file" name="Filedata">  
                  <div id="file_uploadQueue" class="uploadifyQueue"></div>  
                  <p><span id="id_span_msg"></span></p>  
              </div> 
-->
              <div class="item2 mt15">
                <span class="reg-tit3">上传附件：</span>
                  <div class="reg-inf">
                    <input type="file" name="uploadFile" id="uploadFile" class="pub-btn" />  
                      <div id="fileQueue"></div>
                      <a href="javascript:$('#uploadFile').uploadify('upload','*')">上传文件</a>

                      <a href="javascript:$('#uploadFile').uploadify('cancel', '*')">取消所有上传</a>
                  </div> 
                      <label id="showFile" name="showFile"></label>
               </div>
                   <div class="box-body">
<!--
                <div class="form-group">
                  <label for="id_update_dir">update_dir</label>
                  {{ form.update_dir }}
                                  {{ field.errors }}
                </div>
-->
                <div class="form-group">
                  <label for="id_salt_command">salt_command</label>
                  {{ form.salt_command }}
                                  {{ field.errors }}
                </div>
<!--
                                <div class="form-group">
                  <label for="id_salt_host">salt_host</label>
                  {{ form.salt_host }}
                                  {{ field.errors }}
                </div>
-->
<!--
                                <div class="form-group">
                  <label for="id_server_list">server_list</label></br>
                  {{ form.server_list }}
                                  {{ field.errors }}
                </div>
-->
<!--
				<div class="form-group">
                  <label for="serverList">server_list</label></br>
                  {{ form.server_list }}
                                  {{ field.errors }}


		</div>
-->
              <div class="form-group">
<!--去掉multiple以及后面的会产生下拉框[multiple="multiple" name="a" size="2"]-->
              <label for="id_server_list">操作类别 </label>
<!--
                  <option value="cp.get_file">文件更新 cp.get_file</option>
                  <option value="cp.get_dir">文件夹更新 cp.get_dir</option>
-->
                  <p>{{ salt_select_list_all.salt_command_select }}</p>

              </div>
                          <!--
                                <input type="checkbox" value="1" name="server"/>server1</br>
                                <input type="checkbox" value="2" name="server"/>server2</br>
                                <input type="checkbox" value="3" name="server"/>server3</br>
                          -->

			<ul id="tree" class="ztree" style="width:560px; overflow:auto;"></ul>
<!--
			<div class="box-footer">
			    <form method="post" enctype="multipart/form-data" >
				<button type="submit" class="btn btn-primary" id="register">Yes</button>
			    </form>
			</div>
-->
		  </div>
		</div>
<!--
	  </div>
-->

<!--                          <input type="submit" value="submit"/> 
-->
<!--输入一些内容，后台用name提取
                          <input type="text" name="name" id="register">
-->
                          <button type="submit" class="btn btn-primary" id="register">Submit</button>


            </form>

	</section>
	<br/><br/><br/><br/><br/><br/><br/><br/><br/><br/><br/><br/><br/><br/><br/><br/><br/><br/><br/><br/><br/><br/><br/><br/><br/><br/><br/><br/><br/>
  </div>
</div>
  <!-- /.content-wrapper -->


<script>
    //----------------------------------------------------------
    var zTree;
    var demoIframe;

    function addHoverDom(treeId, treeNode) {
        var sObj = $("#" + treeNode.tId + "_span");
        if (treeNode.editNameFlag || $("#addBtn_"+treeNode.tId).length>0) return;
        var addStr = "<span class='button remove' id='removeBtn_" + treeNode.tId
                + "' title='add node' onfocus='this.blur();'></span>";

        addStr += "<span class='button add' id='addBtn_" + treeNode.tId + "'></span>";
        addStr += "<span class='button edit' id='editBtn_" + treeNode.tId + "'></span>";
        sObj.after(addStr);
        var btn = $("#addBtn_"+treeNode.tId);
        if (btn) btn.bind("click", function(){
            var zTree = $.fn.zTree.getZTreeObj("treeDemo");
            zTree.addNodes(treeNode, {id:(1000 + newCount), pId:treeNode.id, name:"new node" + (newCount++)});
            return false;
        });
    };

    function removeHoverDom(treeId, treeNode) {
        $("#addBtn_"+treeNode.tId).unbind().remove();
        $("#removeBtn_"+treeNode.tId).unbind().remove();
        $("#editBtn_"+treeNode.tId).unbind().remove();
    };

    var setting = {
        //async useless
        //async: {
        //    enable: true,
        //},
        check: {
            enable: true,
            chkboxType: { "Y": "ps", "N": "ps" }
        },
        view: {
            addHoverDom: addHoverDom,
            removeHoverDom: removeHoverDom,
            dblClickExpand: true,
            showLine: true,
            selectedMulti: true
        },
        data: {
            simpleData: {
                enable:true,
                idKey: "id",
                pIdKey: "pId",
                rootPId: ""
            }
        },
        callback: {
            beforeClick: function(treeId, treeNode) {
              console.log(222222)
            },
            onClick: function(treeId, treeNode) {
                alert(treeNode.tId + ", " + treeNode.name);
                console.log(333333333)
            },
            onCheck: function(treeId, treeNode) {
                alert(treeNode.tId + ", " + treeNode.name);
                console.log(66666666);
                var treeObj = $.fn.zTree.getZTreeObj("tree");
                //var checkedNodes = treeObj.getSelectedNodes();
                var checkedNodes = treeObj.getCheckedNodes(true);
                var nodes = treeObj.getNodes();
                for (var i =0, l=checkedNodes.length; i<l;i++){
                    console.log(checkedNodes[i]);
                }
                var data={};
                data["jsonTree"] = JSON.stringify(nodes,['name','checked','children']);
                console.log(data["jsonTree"]);
                console.log(000000);
                console.log(9999999999);
                console.log(1 +  "2" + "2");
                console.log(1 +  +"2" + "2");
                console.log(1 +  -"1" + "2");
                console.log(+"1" +  "1" + "2");
                console.log( "A" - "B" + "2");
                console.log( "A" - "B" + 2);
            },
        }
    };

    //function getAllChangeNodes(treeId, treeNode) {
    //   console.log(1999999999991);
    //   var treeObj = $.fn.zTree.getZTreeObj("tree");
    //   var nodes = treeObj.getCheckedNodes(true);
    //   //console.log(nodes);
    //   $('#mytest').html(JSON.stringify(nodes));
    //   var data={};
    //   data["jsonTree"] = JSON.stringify(nodes);
    //   console.log(data["jsonTree"]);
    //}

    function zTreeOnClick(event, treeId, treeNode) {
        categoryTree.selectNode(treeNode);
        //这里可以根据需要，添加其他事件。
    };

    //var zNodes =[
    //    {id:1, pId:0, name:"192.168.30.147"},
    //    {id:101, pId:1, name:"gameserver1 v1.1"},
    //    {id:102, pId:1, name:"gameserver2 v1.1"},
    //    {id:103, pId:1, name:"gameserver3 v1.1"},
    //    {id:104, pId:1, name:"gameserver4 v1.1"},
    //    {id:105, pId:1, name:"gameserver5 v1.1"},

    //    {id:6, pId:0, name:"192.168.30.148"},
    //    {id:601, pId:6, name:"gameserver11 v1.2"},
    //    {id:602, pId:6, name:"gameserver12 v1.2"},
    //    {id:603, pId:6, name:"gameserver13 v1.2"}
    //];
    var zNodes = {{ List|safe }};

    $(document).ready(function(){
        var t = $("#tree");
        t = $.fn.zTree.init(t, setting, zNodes);
        demoIframe = $("#testIframe");
        demoIframe.bind("load", loadReady);
        var zTree = $.fn.zTree.getZTreeObj("tree");
        //zTree.selectNode(zTree.getNodeByParam("id", 101));
        var a = $('#register');
        a.on('click', function () {
            //alert('Hello!');
            var treeObj = $.fn.zTree.getZTreeObj("tree");
            var checkedNodes = treeObj.getCheckedNodes(true);
            var nodes = treeObj.getNodes();
            for (var i =0, l=checkedNodes.length; i<l;i++){
                console.log(checkedNodes[i]);
            }
            var data={};
            $.ajax({
                url:"/data/",
                data:{data:JSON.stringify(nodes,['name','checked','children'])},
                type:'POST',
                //成功之后操作
                //success:function(arg){
                //    alert(data)
                //}
            });
        });
    //-------------------------------------------------------
        $("#uploadFile").uploadify({
        /*注意前面需要书写path的代码*/
        'swf': '/static/image/uploadify/uploadify.swf',
        'uploader': '/upload_script/', //请求的Action  
        'cancelImg': '/static/image/uploadify/uploadify-cancel.png',
        'method': 'GET',
        'queueID': 'fileQueue', //和存放队列的DIV的id一致   
        'fileObjName': 'uploadFile',//和input的name属性值保持一致就好，Struts2就能处理了     
        'auto': false, //是否自动开始   
        'multi': true, //是否支持多文件上传   
        'buttonText': '上传', //按钮上的文字  
        'simUploadLimit': 10, //一次同步上传的文件数目   
        'sizeLimit': 19871202, //设置单个文件大小限制                  
        'queueSizeLimit': 5, //限制在一次队列中的次数（可选定几个文件）。默认值= 999，而一次可传几个文件有 simUploadLimit属性决定。  
        'fileSizeLimit': 10 * 1024 * 1024, //设置单个文件大小限制，单位为byte ,10M  
        'removeCompleted': true,
        'removeTimeout': 0.5,
        'requeueErrors': true,
        'onUploadProgress' : function(file, bytesUploaded, bytesTotal, totalBytesUploaded, totalBytesTotal,queueBytesLoaded) {  

            $("#result").append("<div>文件\""+file.name+"\"共"+totalBytesTotal+"字节，已上传"+totalBytesUploaded + "字节!</div><br/>");  
            if(totalBytesUploaded==totalBytesTotal){  
            $("#result").append("<div>文件\""+file.name+"\"上传成功！</div><br/>");  
            }  
        },  
        'onUploadComplete': function (file) {
             $("#result").append("<div>文件"+file.name+"上传成功！</div><br/>");  
        },
        'onUploadSuccess': function (file, data, response) {
            if ($("#hidFile").val() == "") {
                $("#hidFile").val(data);
                $("#showFile").text(data);
            } else {
                $("#hidFile").val($("#hidFile").val() + "|" + data + "");
                $("#showFile").text($("#showFile").text() + ";" + data + "");
            }
        },
        'onUploadError': function (file, errorCode, errorMsg, errorString, swfuploadifyQueue) {
            $("#result").html(errorString);  
        },
    });

    });

    function loadReady() {
        var bodyH = demoIframe.contents().find("body").get(0).scrollHeight,
                htmlH = demoIframe.contents().find("html").get(0).scrollHeight,
                maxH = Math.max(bodyH, htmlH), minH = Math.min(bodyH, htmlH),
                h = demoIframe.height() >= maxH ? minH:maxH ;
        if (h < 530) h = 530;
        demoIframe.height(h);
    }
    //console.log(11111111111111112);
</script>
</body>
</html>
